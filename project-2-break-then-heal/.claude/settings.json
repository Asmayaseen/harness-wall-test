{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && output=$(npm run lint 2>&1); status=$?; if [ $status -ne 0 ]; then echo \"$output\" >&2; exit 2; fi",
            "shell": "bash"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && output=$(npm run lint 2>&1); status=$?; if [ $status -ne 0 ]; then echo \"$output\" >&2; exit 2; fi",
            "shell": "bash"
          }
        ]
      }
    ]
  }
}
